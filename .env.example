# Cloudflare Worker API Endpoints
# Default values are already configured, but you can override them here if needed
VITE_CALENDAR_API_URL=https://calendar-hsl.moheeb-698.workers.dev
VITE_FLICKR_API_URL=https://flickrhsl.moheeb-698.workers.dev

# These are the default endpoints used if no environment variables are set
# You only need to modify these if you deploy your own Cloudflare Workers